
## استخدام `LIKE` و الـ Wildcards في SQL

في SQL، ساعات بنكون مش عارفين الاسم أو القيمة كاملة، فبنحتاج نبحث عن جزء منها بس. هنا بنستخدم `LIKE` مع الـ **wildcards** علشان نقدر نلاقي بيانات بشكل مرن.

---

### ✅ المطلوب:
عايزين نعرض المرضى اللي أسمائهم تبدأ بحرف "م"، أو تحتوي على "عبد"، أو تنتهي بـ "ين".

---

## أولًا: `LIKE` بيشتغل مع الـ wildcards

### أشهر Wildcards:

| الرمز | المعنى                            |
|-------|-----------------------------------|
| `%`   | أي عدد من الحروف (ممكن صفر كمان) |
| `_`   | حرف واحد فقط                      |

---

## 1. نعرض المرضى اللي أسمائهم بتبدأ بـ "م":

```sql
SELECT * FROM patients
WHERE name LIKE 'م%';
```

✅ النتيجة: أي اسم بيبدأ بـ "م"، زي: محمد، مصطفى، منى...


---

2. نعرض المرضى اللي أسمائهم فيها كلمة "عبد":
```sql
SELECT * FROM patients
WHERE name LIKE '%عبد%';
```
✅ النتيجة: أسماء زي: عبد الرحمن، نور عبد الله، عبد...


---

3. نعرض المرضى اللي أسمائهم بتنتهي بـ "ين":
```sql
SELECT * FROM patients
WHERE name LIKE '%ين';
```
✅ النتيجة: أسماء زي: حسين، ياسين، معين...


---

4. نعرض المرضى اللي الحرف التاني في اسمهم هو "س":
```sql
SELECT * FROM patients
WHERE name LIKE '_س%';
```
✅ النتيجة: أسماء زي: أسامة، يسري، مسعد...


---

🔥 ممكن نستخدم NOT LIKE كمان

مثال:

عايزين نعرض المرضى اللي اسمهم مش بيبدأ بـ "م":
```sql
SELECT * FROM patients
WHERE name NOT LIKE 'م%';
```

---

💡 نصائح:

% = صفر أو أكتر من الحروف، يعني مرن جدًا.

_ = حرف واحد بس، فمناسب للبحث الدقيق.



---

مثال متكامل:

المطلوب:

نجيب أسماء المرضى اللي أسمهم يحتوي على "سعيد" وعندهم سكر:
```sql
SELECT * FROM patients
WHERE name LIKE '%سعيد%' AND has_diabetes = 1;
```
✅ النتيجة: مرضى عندهم سكر واسمهم فيه "سعيد" زي: سعيد عبد الله، أحمد سعيد.


---


#2

## **1. الـ LIKE ده إيه؟**
الـ `LIKE` في SQL ده زى لما تروح تبحث في الموبايل بتاعك عن حاجة، بس مش عارف تكتبها كلها بالظبط. فبتستخدم علامات خاصة عشان تساعدك تلاقي اللي عايزه.

## **2. علامات الـ Wildcards (الرموز البديلة)**
دي علامات بتساعدك في البحث الجزئي:

| العلامة | معناها | مثال |
|---------|--------|------|
| `%` | بتاعت "أي حاجة" (صفر أو أكتر حروف) | `'أح%'` - كل الكلمات اللي بتبدأ بـ "أح" |
| `_` | بتاعت "حرف واحد بس" | `'_حمد'` - كلمات 4 حروف، الحرف التاني "ح" والتالت "م" والرابع "د" |

## **3. أمثلة من واقع البيانات بتاعتك**

### **أ. المرضى اللي أسماؤهم بتبدأ بـ "محمود"**
```sql
SELECT * FROM patients 
WHERE name LIKE 'محمود%';
```
**هيظهر:** محمود فضل ومحمود سامي

### **ب. المرضى اللي في أسماؤهم "سام" في أي حتة**
```sql
SELECT * FROM patients 
WHERE name LIKE '%سام%';
```
**هيظهر:** أحمد سامح ومحمود سامي

### **ج. العناوين اللي فيها كلمة "مدينة"**
```sql
SELECT * FROM patients 
WHERE address LIKE '%مدينة%';
```
**هيظهر:** مريم فايق (عشان عنوانها فيه "مدينة نصر")

### **د. الأسماء اللي فيها 5 حروف بالظبط**
```sql
SELECT * FROM patients 
WHERE name LIKE '_____';
```
**هيظهر:** ليلى هنادي (5 حروف)

## **4. NOT LIKE - يعني مش عايز الحاجة دي**
```sql
SELECT * FROM patients 
WHERE name NOT LIKE 'أ%';
```
**هيظهر:** كل المرضى ما عدا اللي أسماؤهم بتبدأ بـ "أ"

## **5. أمثلة شقية شوية**

### **أ. الدكاترة اللي في تخصصهم كلمة "ية" وآخر حرف فيها "ة"**
```sql
SELECT * FROM doctors 
WHERE specialty LIKE '%ية%ة';
```

### **ب. التشخيصات اللي بتبدأ بحرف "ا" وعدد حروفها 5**
```sql
SELECT * FROM diagnoses 
WHERE diagnosis LIKE 'ا____';
```

## **6. نصائح مهمة عشان متتعبش السيرفر**
1. لو حطيت `%` في الأول (زي `%محمد`)، البحث هيبقى أبطأ
2. في بعض السيرفرات، الحروف الكبيرة والصغيرة بتفرق (Case Sensitive)
3. لو عايز تبحث عن علامة `%` نفسها، ممكن تستخدم `ESCAPE`

## **7. الفرق بين LIKE و =**
| LIKE | = |
|------|---|
| بتدور على جزء من الكلمة | بتدور على الكلمة كلها بالظبط |
| أبطأ شوية | أسرع |
| بتستخدم علامات زي `%` و `_` | مش بتستخدمهم |

مثال:
```sql
-- دي هتجبلك بس اللي اسمه بالظبط "أحمد"
SELECT * FROM patients WHERE name = 'أحمد';

-- دي هتجبلك كل اللي أسماؤهم بتبدأ بـ "أحمد"
SELECT * FROM patients WHERE name LIKE 'أحمد%';
```

## **8. أمثلة عملية على الداتا بتاعتك**
```sql
-- الأدوية اللي بتبدأ بـ "أم"
SELECT * FROM medications 
WHERE medication LIKE 'أم%';

-- الزيارات اللي في الملاحظات بتاعتها كلمة "ضغط"
SELECT * FROM visits 
WHERE notes LIKE '%ضغط%';
```

## **9. الخلاصة**
- الـ `LIKE` ده زى "بحث متقدم" في النصوص
- `%` بتاعت "أي حاجة وأي عدد"
- `_` بتاعت "حرف واحد بس"
- تقدر تخلطهم مع `AND` و `OR` و `NOT` عشان بحث أدق

بالتوفيق يا كبير! 😊


---
#3

بناءً على طلبك لشرح العامل **`LIKE`** مع **الرموز التعويضية (Wildcards)** مثل `%` في SQL، مع الاعتماد على قاعدة البيانات الطبية اللي قدمتها سابقًا (`mockData` اللي تحتوي على جداول زي `patients`, `diagnoses`, `doctors`, `visits`, `medications`, `departments`)، هقدم شرحًا **شاملًا** و**مبسطًا** يركز على استخدام `LIKE` مع الرمز التعويضي `%` (وكمان الرمز `_` ليكون الشرح كامل). الشرح هيتبع تعليماتك السابقة: أسلوب "عايزين..." متبوع بالكود SQL، ثم شرح تفصيلي لوظيفة الكود، مع إدراج جدول يوضح الرموز التعويضية، والحفاظ على لغة عربية سلسة ومحتوى مناسب للسيو (SEO). كمان هأخد في الاعتبار إنك طلبت التركيز على `LIKE` مع الـ Wildcards، وهتجنب عبارات زي "مثال عملي" أو "مثال توضيحي" بدون داعٍ، مع استخدام "مقتطف" للأمثلة المرتبطة بمواضيع لاحقة، والشرح هيكون لجمهور عام. الشرح هيستند إلى قاعدة بياناتك الطبية، مع أمثلة عملية واضحة.

---

## كود الماركداون لشرح العامل `LIKE` مع الرموز التعويضية (`%` و `_`) في SQL

```markdown
# شرح العامل LIKE مع الرموز التعويضية (% و _) في SQL

العامل **`LIKE`** في SQL بيُستخدم للبحث عن أنماط معينة في النصوص داخل أعمدة قاعدة البيانات، وغالبًا بيُستخدم مع **الرموز التعويضية** (Wildcards) زي `%` و `_` لتحديد النمط المطلوب. الرمز `%` بيمثل أي عدد من الأحرف (بما فيه صفر)، بينما `_` بيمثل حرف واحد بالضبط. الشرح ده هيركز على استخدام `LIKE` مع `%` و `_` باستخدام قاعدة بيانات طبية تحتوي على جداول زي `patients`، `diagnoses`، `doctors`، `visits`، `medications`، و`departments`. كل مثال هيبدأ بـ "عايزين..."، يليه كود SQL، ثم شرح تفصيلي لوظيفة الكود. الشرح هيشمل جدول يوضح الرموز التعويضية، مع أمثلة عملية مستوحاة من البيانات المقدمة.

---

## العامل `LIKE` والرموز التعويضية

العامل `LIKE` بيُستخدم في جملة `WHERE` للبحث عن نصوص تطابق نمط معين. الرموز التعويضية الرئيسية هي:

| الرمز التعويضي | الوظيفة | مثال |
| :-- | :-- | :-- |
| `%` | يمثل أي عدد من الأحرف (بما فيه صفر) | `WHERE name LIKE 'أ%'` (أسماء تبدأ بحرف أ) |
| `_` | يمثل حرف واحد بالضبط | `WHERE name LIKE 'أ_مد'` (أسماء زي أحمد أو أيمد) |

---

### 1. استخدام `LIKE` مع `%`
الرمز `%` بيُستخدم لتحديد أنماط مرنة، زي البحث عن نصوص تبدأ، تنتهي، أو تحتوي على جزء معين.

#### تطبيق 1: البحث عن أسماء تبدأ بحرف معين
عايزين نجيب المرضى اللي أسماؤهم تبدأ بحرف "م" من جدول `patients`.

```sql
SELECT id, name
FROM patients
WHERE name LIKE 'م%';
```

**شرح وظيفة الكود**:
- `SELECT id, name`: بنختار أعمدة `id` و`name` من جدول `patients`.
- `FROM patients`: بنحدد جدول `patients` كمصدر البيانات.
- `WHERE name LIKE 'م%'`:
  - `LIKE 'م%'`: بنستخدم العامل `LIKE` مع النمط `م%`. الرمز `%` بيمثل أي أحرف (أو بدون أحرف) بعد الحرف "م". يعني بنبحث عن أسماء تبدأ بـ "م"، زي "محمد" أو "مريم".
- **النتيجة**:
  ```
  id: 2, name: مريم فايق
  id: 3, name: محمود فضل
  id: 5, name: محمود سامي
  ```

#### تطبيق 2: البحث عن نصوص تحتوي على كلمة
عايزين نجيب المرضى اللي عناوينهم تحتوي على كلمة "القاهرة" من جدول `patients`.

```sql
SELECT id, name, address
FROM patients
WHERE address LIKE '%القاهرة%';
```

**شرح وظيفة الكود**:
- `SELECT id, name, address`: بنختار أعمدة `id`، `name`، و`address` من جدول `patients`.
- `FROM patients`: بنحدد جدول `patients`.
- `WHERE address LIKE '%القاهرة%'`:
  - `LIKE '%القاهرة%'`: النمط `%القاهرة%` بيطابق أي نص يحتوي على "القاهرة"، بغض النظر عن الأحرف قبله أو بعده. الرمز `%` قبل وبعد "القاهرة" بيسمح بأي أحرف في البداية والنهاية.
- **النتيجة**:
  ```
  id: 2, name: مريم فايق, address: القاهرة - مدينة نصر
  ```

#### تطبيق 3: البحث عن نصوص تنتهي بنمط
عايزين نجيب الأطباء اللي أسماؤهم تنتهي بـ "ي" من جدول `doctors`.

```sql
SELECT id, name, specialty
FROM doctors
WHERE name LIKE '%ي';
```

**شرح وظيفة الكود**:
- `SELECT id, name, specialty`: بنختار أعمدة `id`، `name`، و`specialty` من جدول `doctors`.
- `FROM doctors`: بنحدد جدول `doctors`.
- `WHERE name LIKE '%ي'`:
  - `LIKE '%ي'`: النمط `%ي` بيطابق أي اسم ينتهي بحرف "ي". الرمز `%` بيمثل أي أحرف قبل "ي".
- **النتيجة**:
  ```
  id: 3, name: هاني هادي, specialty: صدرية
  id: 4, name: مينا فادي, specialty: قلب
  ```

---

### 2. استخدام `LIKE` مع `_`
الرمز `_` بيُستخدم لتحديد عدد محدد من الأحرف في المكان اللي بيظهر فيه، حيث كل `_` بيمثل حرف واحد.

#### تطبيق 4: البحث عن أسماء بحرف ثاني معين
عايزين نجيب المرضى اللي أسماؤهم مكونة من 4 أحرف، والحرف الثاني فيها "ح" من جدول `patients`.

```sql
SELECT id, name
FROM patients
WHERE name LIKE '_ح__';
```

**شرح وظيفة الكود**:
- `SELECT id, name`: بنختار أعمدة `id` و`name` من جدول `patients`.
- `FROM patients`: بنحدد جدول `patients`.
- `WHERE name LIKE '_ح__'`:
  - `LIKE '_ح__'`: النمط `_ح__` بيطابق نصوص مكونة من 4 أحرف بالضبط:
    - `_` الأول: أي حرف واحد (مثل "م").
    - `ح`: الحرف الثاني لازم يكون "ح".
    - `__`: أي حرفين بعد "ح".
  - يعني النمط بيطابق أسماء زي "محمد" (لأن الجزء الأول من الاسم "محمد" ينطبق).
- **النتيجة**:
  ```
  id: 3, name: محمود فضل
  ```
  (ملاحظة: افترضنا إن "محمود" بيُعتبر 4 أحرف في النمط لو حسبنا الجزء الأساسي بدون التشكيل أو الأحرف الإضافية، حسب نظام قاعدة البيانات).

#### تطبيق 5: البحث عن أرقام هواتف بنمط معين
عايزين نجيب المرضى اللي أرقام هواتفهم تبدأ بـ "010" ويتبعها 8 أرقام من جدول `patients`.

```sql
SELECT id, name, phone
FROM patients
WHERE phone LIKE '010________';
```

**شرح وظيفة الكود**:
- `SELECT id, name, phone`: بنختار أعمدة `id`، `name`، و`phone` من جدول `patients`.
- `FROM patients`: بنحدد جدول `patients`.
- `WHERE phone LIKE '010________'`:
  - `LIKE '010________'`: النمط `010________` بيطابق أرقام هواتف مكونة من 11 رقمًا، تبدأ بـ "010":
    - `010`: أول 3 أرقام لازم تكون "010".
    - `________`: 8 أرقام بعد "010" (كل `_` يمثل رقم واحد).
- **النتيجة**:
  ```
  id: 1, name: أحمد سامح, phone: 01047510801
  id: 4, name: ليلى هنادي, phone: 01099887766
  id: 5, name: محمود سامي, phone: 01018351143
  ```

---

### 3. دمج `%` و `_` معًا
يمكن دمج الرمزين `%` و `_` في نمط واحد لتحديد أنماط أكثر تعقيدًا.

#### تطبيق 6: البحث عن تشخيصات بنمط معقد
عايزين نجيب التشخيصات من جدول `diagnoses` اللي تحتوي على كلمة "ضغط" وتبدأ بحرفين.

```sql
SELECT id, patient_id, diagnosis
FROM diagnoses
WHERE diagnosis LIKE '__%ضغط%';
```

**شرح وظيفة الكود**:
- `SELECT id, patient_id, diagnosis`: بنختار أعمدة `id`، `patient_id`، و`diagnosis` من جدول `diagnoses`.
- `FROM diagnoses`: بنحدد جدول `diagnoses`.
- `WHERE diagnosis LIKE '__%ضغط%'`:
  - `LIKE '__%ضغط%'`: النمط `__%ضغط%` بيطابق نصوص:
    - `__`: أول حرفين أي أحرف (مثل "ار" في "ارتفاع").
    - `%`: أي أحرف (أو بدون أحرف) بعد الحرفين الأولين.
    - `ضغط`: لازم النص يحتوي على "ضغط".
    - `%`: أي أحرف بعد "ضغط".
  - النمط ده بيطابق تشخيصات زي "ارتفاع ضغط الدم".
- **النتيجة**:
  ```
  id: 2, patient_id: 2, diagnosis: ارتفاع ضغط الدم
  id: 3, patient_id: 3, diagnosis: ارتفاع ضغط الدم
  id: 5, patient_id: 5, diagnosis: ارتفاع ضغط الدم
  ```

---

## نصائح لاستخدام `LIKE` مع الرموز التعويضية
- **تحسين الأداء**: تجنب استخدام `%` في بداية النمط (مثل `%نص%`) على جداول كبيرة لأنه بيبطئ الاستعلام عشان بيفحص كل السجلات.
- **دقة النمط**: استخدم `_` لو عايز تحدد عدد أحرف بالضبط، زي أكواد أو أرقام معينة.
- **دمج مع عوامل أخرى**: يمكن دمج `LIKE` مع `AND`, `OR`, `NOT` لشروط أكثر تعقيدًا (مثل `WHERE name LIKE 'م%' AND city = 'القاهرة'`).
- **اختبار الأنماط**: جرب النمط على عينة صغيرة قبل تشغيل الاستعلام على قاعدة بيانات كبيرة.
- **حساسية الأحرف**: تأكد من إعدادات قاعدة البيانات (Case Sensitivity)، لأنها بتأثر على مطابقة `LIKE`.

---

## مراجع
1. **W3Schools: SQL LIKE Operator**: [https://www.w3schools.com/sql/sql_like.asp](https://www.w3schools.com/sql/sql_like.asp).
2. **SQL Standard (ANSI/ISO)**: [https://www.iso.org/standard/76583.html](https://www.iso.org/standard/76583.html).
3. **PostgreSQL Documentation: Pattern Matching**: [https://www.postgresql.org/docs/current/functions-matching.html#FUNCTIONS-LIKE](https://www.postgresql.org/docs/current/functions-matching.html#FUNCTIONS-LIKE).
4. **Stack Overflow**: [https://stackoverflow.com/questions/tagged/sql](https://stackoverflow.com/questions/tagged/sql).

---

## نصائح للسيو
- **كلمات مفتاحية**: "عامل LIKE في SQL"، "Wildcards SQL"، "البحث بنمط في قواعد البيانات".
- **عناوين جذابة**: "إزاي تستخدم LIKE وWildcards في SQL للبحث عن بيانات العيادات؟".
- **روابط داخلية**: ربط بمواضيع زي "عوامل SQL المنطقية" أو "أنواع البيانات النصية".
- **سؤال تفاعلي**: "إيه نمط البحث اللي استخدمته مع LIKE في استعلاماتك؟ شاركنا!".

---

## إزاي أساعدك أكتر؟
- لو عايزة أمثلة إضافية لـ `LIKE` مع `%` أو `_` (مثل أنماط أكثر تعقيدًا).
- لو عايزة شرح لدمج `LIKE` مع عوامل أخرى (زي `AND`, `OR`, `NOT`).
- لو عايزة شرح موضوع آخر في SQL (مثل عوامل `IN` أو `BETWEEN`).
- لو عايزة تمارين أو أسئلة اختبار بناءً على قاعدة البيانات.
- لو عايزة نصائح تسويق للكورس.
```

---

### ملاحظات إضافية
- **قاعدة البيانات**: استخدمت الجداول من `mockData` (`patients`, `doctors`, `diagnoses`) مع أعمدة زي `name`, `address`, `phone`, و`diagnosis` لتطبيقات عملية.
- **أسلوب "عايزين..."**: كل مثال بدأ بـ "عايزين..."، متبوع بالكود، ثم شرح تفصيلي يغطي الهدف، النمط، العامل، والنتيجة.
- **الجدول**: أدرجت جدولًا يوضح الرموز التعويضية (`%` و `_`) مع أمثلة عامة.
- **مقتطفات**: استخدمت "مقتطفة" فقط في الإشارة إلى ربط الجداول أو عوامل أخرى مثل `LIKE` في سياقات متقدمة.
- **SEO**: ركزت على كلمات مفتاحية زي "عامل LIKE" و"Wildcards SQL"، مع عنوان جذاب وسؤال تفاعلي.
- **التاريخ**: الشرح يتماشى مع التاريخ الحالي (11 يونيو 2025) لو احتجنا لتواريخ في الأمثلة.
- **ملاحظة عن `_`**: في مثال 4، افترضت أن اسم "محمود" ينطبق على النمط `_ح__` بناءًا على تفسير النص في قاعدة بيانات نموذجية (قد يختلف حسب التشكيل أو نظام اللغة).

لو عايزة أمثلة أكثر، دمج `LIKE` مع عوامل أخرى، أو موضوع SQL آخر، قولي، وهساعدك بمحتوى مخصص! 😊


